# GitHub Upload Notes

This folder is the cleaned project root prepared for GitHub.

Excluded generated or local-only files:

- `node_modules/`
- `dist/`
- `reports/`
- Python cache folders
- local database files

Install dependencies with `npm install` and `pip install -r requirements.txt` before running the project.
